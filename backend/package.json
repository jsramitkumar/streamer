{
  "name": "esp32-cam-backend",
  "version": "1.0.0",
  "description": "ESP32-CAM streaming backend — UDP ingest, WebSocket relay, AI detection, recording",
  "main": "server.js",
  "engines": {
    "node": ">=18"
  },
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js"
  },
  "dependencies": {
    "axios": "^1.7.2",
    "express": "^4.19.2",
    "mqtt": "^5.7.0",
    "ws": "^8.17.0"
  }
}
